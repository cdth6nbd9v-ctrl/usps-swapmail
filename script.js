alert("JavaScript is working!");

// Sample data and basic functions
const sampleTransfers = [
    { current: "New York, NY", desired: "Los Angeles, CA", details: "Test transfer" }
];

function renderTransfers() {
    console.log("Transfers rendered");
}

function showLoginModal() {
    alert("Login modal should open - click OK");
}

document.addEventListener('DOMContentLoaded', () => {
    console.log("✅ SwapMail loaded successfully!");
    renderTransfers();
});alert("JavaScript is working!");

// Sample data and basic functions
const sampleTransfers = [
    { current: "New York, NY", desired: "Los Angeles, CA", details: "Test transfer" }
];

function renderTransfers() {
    console.log("Transfers rendered");
}

function showLoginModal() {
    alert("Login modal should open - click OK");
}

document.addEventListener('DOMContentLoaded', () => {
    console.log("✅ SwapMail loaded successfully!");
    renderTransfers();
});
